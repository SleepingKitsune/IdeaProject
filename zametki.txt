Что можно было бы добавить в приложение:
    Express.js (express: ^4.18.3) — фреймворк для быстрого создания REST API. Упростит маршрутизацию и обработку запросов.

    bcrypt (bcrypt: ^5.1.1) — для безопасного хеширования паролей.

    jsonwebtoken (jsonwebtoken: ^9.0.2) — для реализации JWT‑авторизации.

    class‑validator (class-validator: ^0.14.1) — для валидации входных данных (форм).

    Настроить миграцию для TypeORm